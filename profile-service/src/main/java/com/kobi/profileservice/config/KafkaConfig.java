package com.kobi.profileservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaConfig {
    // Spring Boot sẽ tự động cấu hình DefaultErrorHandler và DLT
    // dựa trên các thuộc tính trong application.yaml.
    // Không cần cấu hình thủ công ở đây.
}